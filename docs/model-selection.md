# Model selection

Production default runtime is llama.cpp's OpenAI-compatible server. Ollama remains an operational
comparison. Candidate 1B–4B Q4_K_M/Q5_K_M models and 1k–8k contexts must be measured on the target.
No primary model is selected until those results exist (`pending-device-validation`).
